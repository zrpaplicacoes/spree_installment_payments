<% if order.interest.exists? %>

    <tr class="warning total total-with-interest">
      <td colspan="4" align="right">
        <b><%= I18n.t('spree.adjustments.interest_adjustment_label', interest: (order.interest.retrieve_percentage) ) %></b>
      </td>
      <td class="total">
        <span class="lead text-primary"><%= order.interest.order_total_with_interest.format %></span>
      </td>
    </tr>

<% end %>

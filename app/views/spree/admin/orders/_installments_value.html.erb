<th> <%= order.payment.nil? ? 1 : order.payment.installments %> </th>
<th> <%= order.payment.nil? ? content_tag(:span, '-', class: "label label-considered_safe") : order.payment.charge_interest_label %> </th>
